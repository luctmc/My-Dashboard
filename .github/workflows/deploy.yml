name: Vercel Deploy Reference

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment note
        run: echo "Connect this repository to Vercel for automatic production deployments."
